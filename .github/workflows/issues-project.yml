name: 🚀 Auto Add Issue And PR To Project
on:
  issues:
    types:
      - opened
jobs:
  add-to-project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@v0.4.1
        with:
          project-url: https://github.com/users/ding77/projects/10
          column: Todo
          github-token: ${{ secrets.GH_PROJECT_TOKEN }}
